import { Request, Response } from 'express';

export const countAccountingRecords = async (req: Request, res: Response) => {
};

export const addAccountingRecord = async ( req: Request, res: Response ) => {
};

export const findAccountingRecord = async ( req: Request, res: Response ) => {
};

export const findAccountingRecords = async ( req: Request, res: Response ) => {
};

export const findAccountingRecordsByFilter = async ( req: Request, res: Response ) => {
};

export const editAccountingRecord = async ( req: Request, res: Response ) => {
};

export const removeAccountingRecord = async ( req: Request, res: Response ) => {
};

export const removeAccountingRecords = async ( req: Request, res: Response ) => {
};